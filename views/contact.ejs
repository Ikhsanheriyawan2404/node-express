
<div class="container mt-3">
    <div class="row">
        <div class="col-lg-12">
            <a href="/contact/create" class="btn btn-primary btn-sm mb-3">Tambah Kontak <i class="bi bi-plus-circle"></i></a>

            <% if (msg.length !== 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <strong><i class="bi bi-check-circle-fill"></i> Success !</strong> <%= msg %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <table class="table table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th scope="col">No</th>
                        <th scope="col">NIK</th>
                        <th scope="col">Name</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach((value, i) => { i++ %>
                        <tr>
                            <th scope="row"><%= i++ %></th>
                            <td><%= value.nik %></td>
                            <td><%= value.name %></td>
                            <td>
                                <a href="/contact/<%= value.nik %>" class="btn btn-sm btn-primary"><i class="bi bi-info-circle"></i></a>
                                <a href="/contact/<%= value.nik %>" class="btn btn-sm btn-success"><i class="bi bi-pencil"></i></a>
                                <a href="/contact/delete/<%= value.nik %>" class="btn btn-sm btn-danger" onclick="return confirm('Yakin?')"><i class="bi bi-trash"></i></a>
                            </td>
                        </tr>
                    <% }) %>
                    </tbody>
              </table>
        </div>
    </div>
</div>